Copy the [NIM_CONNECTOR_ROOT]/plugins/3dsMax/install/nim_3dsMax.ms file to the [3ds Max Install Directory]/scripts/Startup directory

Edit the nim_3dsMax.ms file and replace [NIM_CONNECTOR_ROOT] with the full path to the NIM Connector root folder.